import React from 'react'

function Navigation() {
  // const isvalid=useValid();
  return (
    // isvalid?<Navigate to='dahsboard'/>:<Navigate to='/'/>
    <div>Navigate</div>
  )
}

export default Navigation